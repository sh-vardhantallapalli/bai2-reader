---
name: Run PyTests

on:
  pull_request_review:
    types: [submitted]
  pull_request:
    types: [opened, reopened, edited, synchronize]


jobs:
  docs:
    runs-on: ubuntu-latest
    steps:
      - name: Run PyTests
        run: echo "Running PyTests"
